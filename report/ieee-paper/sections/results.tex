\section{Results}
% Tables and figures summarizing all tasks.
% Highlight trade-offs: performance vs. efficiency vs. stability.

For the graph classification tasks, we present the performance of each embedding method across the three TUDatasets.
We report accuracy, F1-score, and AUC for each method, along with computational metrics such as training time and memory usage.
% The results are summarized in Tables ~\ref{tab:classification_results} and ~\ref{tab:computational_metrics}.
